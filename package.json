{
  "name": "smart-hostel-management",
  "version": "1.0.0",
  "description": "Smart Hostel Management System - MERN Stack",
  "main": "index.js",
  "scripts": {
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "seed": "cd backend && node seedAdmin.js"
  },
  "keywords": [
    "mern",
    "hostel",
    "management",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
